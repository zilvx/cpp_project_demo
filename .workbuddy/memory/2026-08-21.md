# 2026-08-21

## ARB 任意波形「播放」界面：补完为可用播放引擎
- 参考 `/Users/shenxing/Downloads/Playback.html`（Keysight VSG 风格）实现播放界面，代码位于 `src/ui/widgets/ArbWidget.{h,cpp}` 的「播放」标签页。
- 此前该文件从未真正编译过：缺 `#include <QProgressBar>` / `<QFrame>`，导致播放页一直报错。已补上 `<QProgressBar>`、`<QFrame>`、`<QInputDialog>`、`<QList>`。
- 新增 `QTimer` 驱动的播放状态机：`onTimerTick` 每 100ms 推进进度，支持无限(∞)/有限(Nx)循环、列表循环(`advanceSegment`)。
  - 播放/暂停/停止按钮真正驱动进度条与时间线；「下一段」/点击列表行切换当前段；「添加」用 QInputDialog 追加条目；「同步播放」按列表顺序循环并触发 Marker。
  - 构造时 `startPlayback()` 默认进入播放态，复刻设计稿「播放中」外观。
- 圆形按钮配色对齐设计稿：蓝(播放 `#2a6f9c`) / 橙(暂停 `#f39c12`) / 红(停止 `#e74c3c`)，改在 `src/ui/styles/arb_widget.qss`。
- 未实现（非本次范围，属「波形」标签页的库管理）：波形下载/新建/导入/编辑/复制/删除/预览，仍在 `ArbWidget.cpp` 中以「功能待实现」占位。
- 构建：`cmake --build build --target qt_table_app`（工具链 `build/Release/generators/conan_toolchain.cmake`）。
