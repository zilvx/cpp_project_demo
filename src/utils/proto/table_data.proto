syntax = "proto3";

package table_data;

message TableData {
    repeated string columns = 1;
    repeated bool   editable = 2;
    repeated Row    rows = 3;

    message Row {
        repeated string cells = 1;
    }
}
